# Session 003 — 2026-03-07

## What Happened

Defined the architecture layering roadmap (6 layers, 0-5). Added skeleton vs plugin classification to all components. Cross-referenced all docs where connections add value — no orphaned docs, no repeated context.

## Decisions Made

1. **6-layer architecture roadmap** — Layer 0 (Goals/Philosophy) → Layer 1 (System Overview) → Layer 2 (Component Architecture) → Layer 3 (Interactions/Protocols) → Layer 4 (Implementation Design) → Layer 5 (Implementation). Each layer must be solid before moving to the next.
2. **Skeleton vs plugin classification** — all 8 components classified. Core (daemon, orchestrator, registry, task engine, people directory, workspace manager, session/memory, safety) are skeleton. Variable pieces (triggers, comms, LLMs, tools, workflow phases, observability) are plugins.
3. **Cross-references where needed, not everywhere** — docs link to related context only when it helps someone find what they need. No mechanical linking.

## What's Next

Complete Layer 1: component relationships (what calls what) and data flow (what moves through the system). Then Layer 1 is done and we move to Layer 2.
