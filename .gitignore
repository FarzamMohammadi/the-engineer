# Dependencies
node_modules

# Build output
dist/

# Docs site (VitePress) build output + cache
docs/.vitepress/dist/
docs/.vitepress/cache/

# Testing
coverage/

# Runtime data (never commit)
.engineer/
*.db
*.db-wal
*.db-shm

# IDE
.vscode/
.idea/
*.swp
*.swo

# Editor / tooling backups
*.bak

# Environment / secrets (track .example files, ignore real ones)
.env*
!.env*.example

# Personal seed directories (seed-example/ itself stays tracked as a reference)
seed-example-*

# Claude Code local config — track only the shared skills, ignore everything else
.claude/*
!.claude/skills/

package-lock.json

# OS
.DS_Store
Thumbs.db
