# Session 49 — 2026-05-31

Slice 8 (Pipeline Phases) — Implementation Session **9: the project-wide docs sweep** (+ bundled-mirror sync + a full legacy-code audit). Two green commits. This brings the entire documentation surface back in sync with the post-cutover pipeline and verifies the old architecture is gone, not just hidden.

## What Happened

Code is truth; the sweep was docs-only (surface code issues, don't fix them). The first pass reconciled ~19 live docs against the actual pipeline code from the atomic cutover (S5) through HEAD; an independent **43-agent Haiku swarm** then re-verified every live doc against source (each finding re-checked by hand before trusting — it caught a swarm false positive on `cooperative_preemption`, which is live). Per Farzam's directive ("unless they are in `docs/archived/` i want you to fix them"), ALL findings in live docs were fixed, not just the flagged ones.

1. **`d9db73e` Resync the documentation surface to the post-cutover pipeline** (29 files, +582/−1024, squashed from four area commits at Farzam's request).
   - **Architecture:** new `architecture/pipeline.md` (the 6-phase sub-phase design narrative); rewrote `overview.md` to the 6-phase lifecycle + 6-state machine and replaced the `engineer.plugin.yaml` plugin model with the `builtin.ts` manifest object; fixed `three-tier-model.md` (ticket_management capability, `receive` methods, no ToolAdapter, dead links) and the `scheduling-dispatch.md` signal-threading note.
   - **Configuration:** dropped fictional keys (`rrpir.*`, `force_full_pipeline`, `git_token_env`), added the real ones (`review.lenses`, `pr.skip_pr_creation`, `review_polling`, `notification_retry`, `evaluation`), and corrected safety + config-dir wording — every key verified key-by-key against the Zod schemas.
   - **User flows:** ground-up rewrite of `pr-management/overview.md` (delivery sub-phases, the `pr-event-poller`, the typed `PrEvent` vocabulary, entry-driven auto-merge) and `post-execution-review` (lens factory + refine's 5-verdict vocab); stripped decomposition residue from `task-intake`.
   - **Plugins/contribution/README:** agent `run()` + `runContractSuite` names + `max_cli_output_bytes`, git-hosting return shapes + method counts + detectPrEvents/getAuthenticatedRemoteUrl, trigger metadata type; observability (14 observation types, corrected file paths), zod-schemas, the agent-adapter setup prompt; README's 6-phase + auto-merge corrections; writing-tickets' six state labels + `--json created_at`.

2. **`8de3d6c` Sync the bundled plugin-doc mirror with the post-cutover contracts.** `src/cli/bundled/plugin-docs.ts` mirrors `docs/plugins/**` *manually* (per its own header) and had drifted from every S9 source fix. Synced the git-hosting contract table (added `detectPrEvents` + `getAuthenticatedRemoteUrl`, corrected the getPRStatus/getReviewStatus/getBranchProtection return shapes + the facts-vs-policy paragraph), the agent adapter (`infer()`→`run()`, `runAgentContractSuite`→`runContractSuite`), the trigger metadata type, and the github-comm `executing`→`active` label example.

**Legacy-code audit (the "is the old code really gone" review Farzam asked for).** grep + knip + direct schema reads confirmed: the legacy runner / phase-handlers / agent-runner / phase-navigator / andon-cord / review-handler / session-result are deleted (0 files, 0 refs); all dead symbols, events, and enum values are gone; `pr-manager` is reduced to `removeThoughtsAndPush`; knip is clean; zero TODO/FIXME/HACK in the backend. The only residue is **vestigial DATA-schema surface knip structurally cannot see** — parsed-but-unread config, write-only fields, and defensive dead branches — all captured in `project_slice8_s10_followups.md` for S10.

**Verify:** typecheck · knip clean · `test:all` (**2206 unit + 47 integration + 16 e2e**) · build — all green. Docs-only + a bundled-string edit, so schema-neutral (no migration/wipe).

## Decisions Made

1. **Fix all live-doc findings, only `docs/archived/` is off-limits** (Farzam's directive). The archived build journal is frozen (forward-only-docs); everything else gets fixed, no "pre-existing / out-of-scope" deferral. Saved as `feedback_fix_all_except_archived`.
2. **Independent swarm verification.** A 43-agent Haiku swarm audited all live docs in parallel for breadth/cost, but every finding was re-verified against source before action — the swarm informs, it does not decide.
3. **The bundled mirror is in S9 scope and is doc content** (the plan listed "bundled CLI docs"). Syncing `plugin-docs.ts` string literals is documentation work, not a code change — fixed directly. Two lower-stakes mirror items (the 3 agent pages' `max_cli_output_bytes` row, github-hosting.md's How-It-Works prose) were left for S10's file-by-file pass, which re-reads that file anyway.
4. **Vestigial schema surface is S10, not S9.** Parsed-but-unread config, write-only fields (`demo_artifacts`/`pr_state`/`FeedbackRound.stage`), and the defensive `error`/`terminated` branches are *code* — surfaced and captured for the closing sweep, not fixed in a docs-only session.

## What's Next

**Session 10 (overall 50) — the CLOSING STANDARDS SWEEP.** Go wide: a genuine, exhaustive, co-owner audit of the full slice diff (98 src/ files), two-pass, against coding-standards / anti-patterns / philosophy, plus clearing the captured follow-ups. Not bounded by the follow-ups memory — surface anything off, including design questions. Temp-branch-from-slice-start + cherry-pick-back is sanctioned; multi-agent fan-out encouraged. Large — may spill to Session 11. Dashboard UI stays out (Slice 13). See `active.md` § "Next session's task" + slice file § "Closing Standards Sweep" + plan § "Session 10".
