# Session 12 — 2026-05-19

## What Happened

One-session tangent to clear two cleanup items before resuming Slice 4 Phase 3. Removed the dead ToolAdapter and BashToolPlugin (orphaned by the CLI-native pivot) — 36 files changed across source, tests, docs, seeds, and memory, dropping from 5 adapter types to 4 (−2182 lines). Upgraded tsdown from 0.12.9 to 0.22.0, fixing the internal `define` schema-mismatch build warning; updated `bin`/`main`/`reset.sh` references for the `.js` → `.mjs` output extension change. Both commits land green (build, lint, 2440 tests, daemon smoke).

## Decisions Made

1. Loading order after ToolAdapter removal: simply remove Tool from the sequence → Communication > LLM > GitHosting > Trigger.
2. tsdown output extension change (`.js` → `.mjs`): update references rather than configure tsdown to emit `.js`. The project already has `"type": "module"` so `.mjs` is correct.
3. Historical implementation-docs referencing `dist/index.js` left as-is (forward-only docs rule).

## What's Next

Resume Slice 4 Phase 3 — OS Detection & Setup UX. Add `detectOperatingSystem()` pure function + tests, wire the OS gate into first-run setup, add detection-summary confirmation, polish setup messaging, update `future-considerations.md`.
