# Session 63 — Slice 10 (Communication) BUILD, S7: AUDIT-2 (docs + bundled-mirror sweep) + finalize

> On `slice10-build`, on top of S6 (`22d310d`). The slice-closing docs sweep per plan § S7 and
> `feedback_slice_closing_standards_sweep` + `feedback_docs_from_source` + `feedback_no_stale_counts`.
> One green commit (`9f79082`) + the finalize commit (this log + active.md + the slice design record).

## Scope

Verify every Slice-10-touched doc against docs-as-system-blueprint and no-stale-counts: paths,
config-keys, defaults, and capability names match the code as it now is (grep-verified, not assumed),
no stale counts. CRITICAL part: diff the two hand-maintained bundled mirrors
(`cli/bundled/plugin-docs.ts`, `cli/bundled/templates.ts`) against their live markdown line-by-line —
they silently drift. Confirm the cut config is gone from BOTH live docs and bundled templates, and the
new behavior (suppression, decision-escalation/autonomy categories, cancel label, inbound query) is
documented in both. Then FINALIZE: active.md reflects Slice 10 as built-and-audited, and write the
durable slice design record.

Slice-10-touched docs (diff vs base `17c43894`): `README.md`, `docs/configuration/safety.md`,
`docs/configuration/orchestrator.md`, `docs/plugins/communication/README.md`,
`docs/plugins/communication/telegram-comm.md`, `docs/future-considerations.md`,
`docs/usage-guide/writing-tickets.md`. Mirrors: `cli/bundled/{plugin-docs,templates}.ts`.

## Method

Extracted each bundled template-literal block (unescaping `\`` and `\\`) and `diff`-ed it byte-for-byte
against its live markdown source. Grepped the autonomy category vocabulary across all four sources that
must teach the same set. Grepped every cut-config token across `docs/` + templates + schema. Spot-checked
every doc claim against the real code (suppress key, query vocabulary, the `[Task: …]` tag, the progress/
cost formatters, the "couldn't match" wording).

## Verdict

**Slice-10 docs are accurate and in sync.** The new behavior is documented in BOTH the live docs and the
bundled mirrors; the cut config is fully gone from BOTH. Confirmed in code-vs-doc:

- **Cut config gone.** No live `OrchestratorConfig.notification` / `question_batching` anywhere —
  schema, both orchestrator templates, `orchestrator.md`. `OrchestratorConfig` = `{review,
  observability}`; `orchestrator.md` correctly points notification controls at `daemon.md`
  (suppression + retry).
- **Autonomy categories synced across four sources** — `DEFAULT_AUTONOMY_DECISIONS` (config.ts), the two
  safety templates (templates.ts), the prompt vocabulary (agent-prompt.ts, which carries an explicit
  sync comment), and `safety.md`'s "Default categories" table: same 11 categories, same levels, same
  `files > 5` thresholds, same descriptions.
- **Suppression** — `daemon.md`'s "Notification Suppression" section matches the code dedup key
  `${kind}:${scope}` (taskId, or alert `source` for null-task alerts).
- **Inbound query** — README "Inbound queries" table + "Query vs. unblock reply" ordering match
  `query-handler.classifyQuery` + the poller's `classifyInbound` precedence (query-keyword wins on the
  single-blocked case); the `progress #N`/`cost`/"couldn't match — N are blocked" text matches the
  formatters; `status_response` resolves to the owner.
- **Smart-reply correction** — `future-considerations.md`'s `[Task: <8-char-id>]` matches
  `outreach-sender.ts` / `orchestrator/index.ts` (`taskId.slice(0, 8)`); confirmed no inbound parser of
  the tag (correlation is structural).
- **Cancel label** — `writing-tickets.md` documents `engineer:cancelled` (no bundled mirror exists for
  it). README autonomy section change is accurate vs `safety.md` + code.
- **Telegram mirror is byte-identical** to the live `telegram-comm.md` (the S5 "Querying from Telegram"
  note carried correctly).

## Findings + fixes (commit `9f79082`)

Four mirror/template drifts fixed — docs only, no behavior change:

1. **`plugin-docs.ts` "mirrored trees" line** (all three adapter READMEs) — missing the coding-standards
   § 7 link the live docs gained in the Slice 5 sweep. *Pre-existing (Slice 5 `de7b7e9`), not
   Slice-10-introduced.* Synced.
2. **`plugin-docs.ts` github-comm "label management is best-effort"** — mirror still said the error is
   "swallowed"; the live doc says "logged at `warn` and treated as non-fatal." *Pre-existing (Slice 5
   `60c4e63`).* Synced.
3. **`templates.ts` notification_suppress_window_ms comment** (DAEMON_TEMPLATE + EXAMPLE_DAEMON) — said
   "same type + scope"; the router keys dedup on the notification's **kind**, and `daemon.md` says
   "kind." *Slice-10-touched (S1).* Aligned to "kind."
4. **Orchestrator templates missing the `review` block** — both ORCHESTRATOR_TEMPLATE and
   EXAMPLE_ORCHESTRATOR ("Full Reference") omitted `review.lenses`, which `orchestrator.md` documents
   with an example. *Pre-existing gap (the `review` config was never templated), surfaced in a
   Slice-10-touched file.* Added (commented in the minimal template, populated in the example). YAML-
   parse-verified: EXAMPLE_ORCHESTRATOR → `{review:{lenses:[self-review]}, observability:{...}}`, a
   valid `OrchestratorConfig`.

## Flagged for the owner (out of AUDIT-2 scope — code changes, not docs sync)

- **Per-person `quiet_hours` + `notification_level`** (`PersonSchema.preferences` in `adapters.ts`;
  documented in `people.md` + the people templates; written by `setup.ts`) is **fully unwired dead
  scaffolding** for the CUT quiet-hours/notification-level features — `grep` confirms zero code
  consumers. Deleting it is a code change (schema + setup + tests + docs) beyond a docs-mirror audit;
  flagged by S1 and S2, still standing. Recommend deleting in the owner's final code sweep before merge.
- **`AGENT_README` bundled mirror has substantial pre-existing drift** from the live
  `docs/plugins/agent/README.md` (stale intro paragraph; missing the `trace_output_path` and `signal`
  `AgentRunOptions` rows; em-dash differences). NOT a Slice-10 doc (the agent adapter is not
  Slice-10-touched) — fixing the whole agent-README mirror would balloon AUDIT-2 well past its charge.
  My shared-line edit (#1 above) reduced this drift by one line; the rest is flagged for a dedicated
  agent-docs sync. (The communication, telegram, and github-comm mirrors are now byte-identical to
  their live sources; the trigger mirror is identical too.)

## Reality-vs-research

No new discrepancies. The S5/S6 notes (answer-injection return path already on main `68a7`; the
`[Task: …]` tag appended-on-send-never-parsed-on-receive) hold and are reflected in code + docs.

## Gate results

- **Lint:** PASS — `biome check` clean (475 files), `tsc --noEmit` clean ×2, `knip` clean, `madge
  --circular` clean.
- **Typecheck:** PASS (exit 0).
- **Tests:** PASS — 2542 unit + 64 integration + 16 e2e, all green (unchanged from S6; docs/template
  edits change no behavior; `setup.test.ts`, which validates templates, passes).

## Finalize

- `active.md` updated: Slice 10 marked **built-and-audited, pending the owner's final review + merge**.
- Durable slice design record written: `slices/10-communication.md` (the 3 pillars as built, locked
  decisions, the refine-over-build cuts, cross-slice handoffs, the closing-audit verdict, a lens check).

## Next

The owner's final hands-on review of `slice10-build` (read every commit + diff against the plan +
coding-standards/anti-patterns/observability + the Definition of Done; re-run gates; hand-check the
acceptance behaviors; resolve the two flagged items above and the alert-bypass-wording note), then
merge `slice10-build` → `main`. **DB wipe is the migration** (Slice 10 cut config + moved the suppress
window): delete `~/.engineer/data/engineer.db` (or `rm -rf ~/.engineer`) before running.
