pre-commit:
  parallel: true
  commands:
    biome-check:
      glob: "*.{ts,json,jsonc}"
      run: pnpm biome check --staged --no-errors-on-unmatched
    type-check:
      run: pnpm tsc --noEmit

pre-push:
  commands:
    test:
      run: pnpm test
