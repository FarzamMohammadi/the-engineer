# Session 22 — 2026-05-21

## What Happened

Executed **Plan Session 5 — the Closing Standards Sweep**, the quality gate that closes Slice 5.
Read every in-scope file (22 Tier-1 source, 6 Tier-2, the new + modified tests, and all new/modified
docs) line-by-line against `coding-standards.md`, `anti-patterns.md`, and `philosophy.md`. The core
logic was already high quality — the real defects clustered in dead code wired to nothing and docs
lying about deleted behavior. Landed seven green commits:

1. **Removed unwired config hot-reload infrastructure** (the carried finding). `createConfigWatcher`,
   the `updateConfig`/`updateLimits` methods on SafetyLayer/PolicyEngine/CostTracker/PeopleDirectory,
   and the `health.config_reload_failed` event were all exercised only by tests — nothing wired the
   watcher into the daemon. Deleted the lot (+ its tests) and corrected every doc/template/seed claim
   to "takes effect on restart." ~615 lines removed.
2. **Re-synced bundled `plugin-docs.ts` with corrected source** — the bundled trigger/telegram docs
   still described file-based watermark/chat-map persistence (deleted in favor of the Core StateStore),
   a removed `poll_interval_ms` config field, and a wrong `labels` default. Also fixed stale "PR reviews"
   cross-refs (source + bundled) and pulled in the github-hosting `dismissApprovals` method the bundled
   copy lagged.
3. **Removed the dead `max_tokens` config field** (declared in claude-code-llm config, never read) +
   doc-accuracy fixes: EXAMPLE_PEOPLE reframed for single-user, cli.md "doctor categories 1-7" stale/wrong
   count fixed, architecture manifest example corrected to `poll_interval_ms`, seed model → `claude-opus-4-7`.
4. **Captured smart reply correlation** (Decision #9 deferral) in `future-considerations.md`.
5. **Fixed the chronic orchestrator test flake** — `createTestOrchestrator` used a shared
   `/tmp/worktree/task-001` path; parallel test files clobbered each other's `session-result.json`.
   Made it unique per worker (`mkdtemp` at module load), exported `TEST_WORKTREE_PATH`, and skip git-init
   when `execFileSync` is mocked. Verified with 5 consecutive green full-suite runs.
6. **Clarified two misleading source comments** (trigger-poller "N+1", task-engine "backward compatibility").

## Decisions Made

1. **Delete the unwired hot-reload infra rather than wire it** (Farzam). Honest code over keeping
   tested-but-dead scaffolding; docs corrected to tell the truth.
2. **Fix stale "PR reviews" cross-refs in source + bundled** (Farzam), even though two source files were
   outside the Slice 5 changeset — honesty over strict scope.
3. **Kill the dead `max_tokens` field** (Farzam) — parsed but never passed to the CLI.
4. **`seed-example/plugins/claude-code-llm.yaml` holds intentional dogfooding values** — I wrongly
   "sanitized" the absolute `cli_path`/model; Farzam corrected that it's deliberate. Saved to memory.
5. **Close Slice 5; track the e2e breakage separately** (Farzam) — Slice 5's scope is met and green;
   the e2e failure predates the slice and gets its own focused session.

## What's Next

**Top priority: the pre-existing e2e breakage.** All five `task-happy-path` + `crash-recovery` e2e tests
fail — the daemon's happy path never reaches the LLM (`fake-llm.getCallCount()` is 0). Verified identical
at `ad7b400` (Slice 5's start), so it predates this slice; it was hidden because `test:all`'s `&&` chain
short-circuited on the (now-fixed) unit flake. Integration tests (40) pass with the same harness, so the
harness works — start in the daemon scheduling/dispatch path. This is its own focused session.

After that: **Slice 6 — Scheduling & Dispatch.**
