# Claude Code agent plugin

# model: <id>         # defaults to claude-sonnet-4-6; set to a model your Claude Code can use
# cli_path: claude    # Path to the Claude Code CLI. Defaults to "claude" on your PATH; set only if it lives elsewhere.
