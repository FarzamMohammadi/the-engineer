# Session 017 — 2026-03-08

## What Happened

Continued Protocols doc (`protocols.md`) with Batch 3: Communication Protocols (P11-P14) and Batch 4: Resilience (P15). Drafted all five protocols — Blocking & Human Interaction, Question Batching & Response Parsing, Notification Delivery, Status Query, and Crash Recovery & System Restart. All 15 protocols are now drafted. Updated daemon-scheduler.md with `shutdown_timeout` config. Added 2 new decisions (#51, #52).

## Decisions Made

1. **LLM fallback for natural language response parsing** (#51) — Try structured parsing first (numbered format), fall back to LLM for natural language. Only LLM usage in the communication path. Daemon query handler stays LLM-free.
2. **Shutdown timeout owned by Daemon config** (#52) — `shutdown_timeout` (default: 30s) lives in Daemon config alongside other process-management parameters. Process lifecycle is Daemon's domain, not Safety Layer's.

## Verification Pass

Cross-referenced all events, contracts, and state transitions in P11-P15 against source docs:
- **16/16 events** valid in event-catalog.md
- **14/14 cross-protocol references** valid
- **3/3 state transitions** valid per task-engine.md
- **2 gaps found and fixed**: added `getBlockedTasksForPerson()` and `getAllExternalRefs()` to task-engine.md Operations, added `"orchestrator_crash"` condition to `health.stuck_detected` event in event-catalog.md

## Files Created/Modified

- **Modified:** `protocols.md` — added sections "3. Communication Protocols" (P11-P14) and "4. Resilience Protocols" (P15)
- **Modified:** `decisions.md` — added decisions #51 and #52
- **Modified:** `daemon-scheduler.md` — added `shutdown_timeout` to DaemonState.config schema
- **Modified:** `task-engine.md` — added `getBlockedTasksForPerson()` and `getAllExternalRefs()` operations
- **Modified:** `event-catalog.md` — added `"orchestrator_crash"` condition to `health.stuck_detected`
- **Modified:** `layers.md` — updated decision count to 52, updated protocols status
- **Created:** `sessions/017.md` — this file
- **Modified:** `active.md` — updated status

## What's Next

Continue with Error Propagation doc (`error-propagation.md`). Then finalize Plugin Contracts (incorporate requirements from protocols + error analysis). Then Lifecycle traces, then mini holistic review.
