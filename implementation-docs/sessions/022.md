# Session 22 — 2026-03-09

## What Happened

Formalized the three-tier architecture (Core → Adapters → Plugins) that was implicit across the docs. Created `architecture-tiers.md` as the canonical Layer 1 reference. Renamed `plugin-contracts.md` to `adapter-contracts.md` with all adapter interfaces renamed to proper names (TriggerAdapter, CommunicationAdapter, LLMAdapter, ToolAdapter, GitHostingAdapter). Swept "skeleton" → "Core" terminology across all L1-L3 docs (16 files). Fixed the stale Event Bus pre-processing reference in relationships.md. Added extensibility design ensuring the adapter tier is open-ended for future evolution.

## Decisions Made

1. #59 — Three-tier architecture: Core / Adapter / Plugin. Adapter tier is open-ended by design.
2. #60 — Explicit adapter naming with full names (CommunicationAdapter, not CommAdapter).
3. #61 — "Skeleton" terminology evolves to "Core." Historical Layer 0 docs retain "skeleton" with evolution note.
4. #62 — Optional adapter methods via capability gates. Core checks capability before calling.
5. #63 — Adapter contracts and plugin implementations co-located in one file (adapter-contracts.md).
6. #64 — Future adapter types (Workflow Phases, Observability Backends) deferred to Layer 4. Adapter tier explicitly open to new types.

## What's Next

Layer 4 — Implementation Design. Technology choices, data structures, file layout, configuration format, deployment specs, testing strategy. The architectural foundation (Layers 0-3, 64 decisions) is complete.
