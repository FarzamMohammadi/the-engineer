# Active: Phase 9 (OSS Ready)

> **ALWAYS READ BEFORE PROCEEDING.** Then read [approach.md](approach.md) and the current slice file.
> These references are permanent. Never remove them.

## Key Files

- [vision.md](vision.md): why we're doing this, what done looks like
- [approach.md](approach.md): strategy, lenses, co-founder rules, RRP discipline (what to hunt for, how to present findings), closing sweep principles, 16-slice roadmap, session protocol
- Current slice: **Slice 8 (Pipeline Phases)** — implementation in progress (Sessions 40-46 done through 7a; ~11 sessions total with the 7a/7b split). RRPIR landed Sessions 38-39; the plan + slice file `slices/08-pipeline-phases.md` are locked and expert-panel-hardened.

## How This File Works

This file answers one question: **where are we right now?** Nothing more.

- **Current** holds the active slice: its state, plan, and the immediate next step.
- When a slice finishes, recap it as **one line** under **Completed Slices**, then advance
  **Current** to the next slice.
- Depth lives elsewhere: per-session detail in `sessions/N.md`, per-slice decisions in that
  slice's file. Do not duplicate that depth here.

## Current

**Slice 8 (Pipeline Phases)**: RRPIR + Implementation Sessions 1-6 COMPLETE. RRPIR landed Sessions 38-39 (plan locked, expert-panel-hardened). Sessions 1-4 (overall 40-43) cleaned the foundation then built the entire new pipeline DARK — core (`{types,runner,agent-step}.ts` + signal threading), upstream phases (`gather`/`investigate`/`design`/`implement`+`verify` + `agent-prompt`/`grounding`), and Review + Delivery (`review/*` lens factory + `refine`'s 5-verdict vocabulary; `delivery/*` shells + `skip_pr_creation` skip-gates; the typed `PrEvent` vocabulary + `entryFor`/`arbitrate`), all proven end to end against real WorkspaceManager + SessionMemory + observer with only the agent faked. **Session 5 (overall 44) did the atomic cutover** — two commits: `71a4089` filled `push`'s live body (commit-stragglers + authenticated push) so push-only delivers end to end, then `3e873f4` pointed `executeTask` at `runPipeline(PIPELINE, ctx)`, deleted the legacy runner + handlers + agent-runner + legacy prompts + `core/session-result` + the `pr-manager` PR cluster + `rrpir` config, finalized `001_schema.sql` (dropped the dead task columns; added `sub_phase`/`phase_iteration`/`total_reworks` to tasks AND checkpoints), moved `BlockCategory` into `schemas/task.ts` beside the renamed `pipeline_failed` reason, reshaped `BlockedDetails` to typed keys, wired the runner's position/counter persistence + resume restore, and folded `agentStep`'s safety-gating/cost/usage bookkeeping (with the real plugin id). All gates green: 2222 unit + 47 integration + 16 e2e, lint 0, build — **`test:all` now runs entirely against the new pipeline**. The legacy runner is gone. **Pre-v1: delete `~/.engineer/data.db` before running this version (the DB wipe is the migration).** **Session 6 (overall 45) made the typed PR-event producer real** — two commits: `7fc771f` added `GitHostingAdapter.detectPrEvents` + the per-variant `PrEvent` payloads (discriminated union, thin — only `pr_comments` carries data), the github-hosting `doDetectPrEvents` aggregating live PR state through a pure `derivePrEvents` (stateless `pr_ready_to_merge` only when approval + CI-green + mergeable hold together), and the Core policy consolidated into `pipeline/pr-events.ts` (`entryFor`/`arbitrate` moved in, `dedupePrEvents` + `findAuthorizedApproval` added); `77aec34` the contract + `constraints.md` docs. 2246 unit + 47 integration + 16 e2e, lint 0, build green. The live `review-handler.ts` is untouched — S7 refactors it onto these events. **Session 7 was pre-split 7a/7b** (Farzam's pick — it strained the budget). **Session 7a (overall 46) is DONE** — four commits: `7eca4d9` the `pending_pr_event` field + `executeTask` re-entry (entryFor cursor + carry seed), `5160af2` the rebuilt `create-pr`, `91eeefb` the `review-handler` → `pr-event-poller` refactor (the 996-line handler is gone), `dad3434` the await-review re-entry cleanup. External rework events now re-enter end to end (comments → requirements, CI failure / merge conflict → execution); 2184 unit + 47 integration + 16 e2e, lint 0 (knip + madge clean), build green. **Session 7b (live auto-merge + `/approve` promotion + the `blocked` consumer audit) is next.**

**Session 2 refinements (see `sessions/41.md`):** (1) the cause enum shipped as a single complete `BlockCategory` (failures + waits, no nullable field) instead of D9's `{reason, category}` — Farzam's steer; coarse `BlockReason` is derived at the S5 wiring. (2) `cli-run.ts` was renamed to **`agent-step.ts`** (`agentStep`) for clarity vs the runner; "CLI sub-phase" → "agent sub-phase". (3) routing is a pure `planRoute` (FCIS); the runner loop is the thin shell. (4) `agent-step` is scoped to the subprocess boundary only — safety-pipeline gating, cost events, and tracking wrap it at the cutover wiring.

**Session 3 decisions (see `sessions/42.md`):** (1) Trivial-complexity flows requirements → research/planning via the **file handoff** — `grounding.ts`'s `readGrounding` reads `requirements/session-result.json`'s `details`; no schema/DB/Ctx change (the task-row mirror is an S5 option). (2) **Grounding is the foundational first step** (Farzam's steer): the agent acclimates to the project and the verification commands fall out of that grounding (recorded in requirements' `details.verification`), not a bolt-on detector. `verify` reads those commands and owns the pass/fail verdict — it runs the gates itself via `execFileSync`, so the agent cannot fake green. (3) Trivial tasks skip **both** research AND planning (slice-locked; legacy skipped research only). (4) New prompts are preview — grounding / context-summary-first / observations-vs-inferences / design-and-stress-test disciplines woven in; the exhaustive dev-toolbox port stays Session 8. Captured the grounding principle to memory (`feedback_grounding_first`, `reference_dev_toolbox_rrpir_skills`).

**Session 4 decisions (see `sessions/43.md`):** (1) **Delivery's live git-hosting interaction is built as dark shells** (Farzam's pick): real routing + skip-gates + observability now; `push`/`createPR`/`mergePR` carry `TODO(farzam)` and land at the S7 wiring (their form depends on the finalized task state at S5 + `detectPrEvents` at S6). `await-review` only routes into the `awaiting_pr_review` wait. (2) **`refine` carries a 5-verdict typed vocabulary** (Farzam's pick): `ship`→advance, `revise`→repeat (cap 3), `rework_execution`/`rework_planning`/`rework_requirements`→jump; totals over the three route verbs (the kickoff's 4-verdict list missed the repeat). (3) The new review-lens enable-list is a **new `OrchestratorConfig.review.lenses` field** (default `["self-review"]`), separate from legacy `rrpir.review_phases` (deleted at S5). (4) **`arbitrate` precedence** (Core policy): `pr_merged` > `pr_comments` > `pr_merge_conflict` > `pr_ci_failure` > `pr_ready_to_merge`. (5) A **lens-factory** compresses the four near-identical lens files (diverges from the slice's "copy security.ts" walkthrough — reconcile in the S9 docs sweep).

**Reference files:**
- Slice file (durable backbone + design narrative for the docs slice): [`slices/08-pipeline-phases.md`](slices/08-pipeline-phases.md)
- Plan (decision record + 10-session breakdown + verification + risks + panel review + pre-mortem): `.claude/temp/create-plan/slice-08-pipeline-phases.md`
- Research artifact: `.claude/temp/research/slice-08-pipeline-phases.md`
- Architecture in memory: `project_slice8_pipeline_architecture.md`

**The locked architecture (one-paragraph summary):** the 7-phase hardcoded pipeline becomes a **6-phase data-driven sub-phase architecture** (requirements → research → planning → execution → review → delivery; integration cut). Folders = phases, files = sub-phases; each sub-phase file owns `run` (do the work) + `next` (where to go), where `next(result,ctx): Route` is a plain function (no routing DSL). The agent reports a 3-word outcome (`ok`/`needs_human`/`failed`) and the orchestrator owns the route. `agent-step.ts` (the `agentStep` helper) is the one defended boundary for the opaque agent subprocess. Two loop counters (`phase_iteration` intra + `total_reworks` inter) on task + checkpoint. External PR events re-enter via DB re-queue + an `entryFor` map (no back-channel call); `pr_ready_to_merge` is computed statelessly (kills the in-memory wait map + its restart bug). Every pipeline failure blocks with a unified typed `BlockDetail` whose `category` is a single complete `BlockCategory` (failures and waits, never null); crashes keep retry-policy's backoff (untouched). Deliverable is config-driven (PR mode = merged PR; push-only = pushed branch). Migration is build-dark-then-atomic-cutover (no parallel runners). Observability is emitted by the runner so a step can't forget it. Slice 8 absorbs original Slices 9 + 10; downstream slices renumber down by 2; ALL dashboard UI deferred to Slice 13.

**RRPIR reshaped the original framing substantially** (vs Session 38's first draft): the "sub-phase registry with RoutingRule[] + predicate DSL" was killed by the expert panel and replaced with the `next()`-function design; auto-merge is preserved (not cut); retry-policy is untouched (crash ≠ pipeline failure); external events route via DB re-queue (not `applyExternalEvent`); migration is atomic cutover (not parallel runners). The slice file + plan reflect the final locked design.

**Session 5 decisions (see `sessions/44.md`):** (1) `pr_workflow_failed` → **`pipeline_failed`** (Farzam's pick) — the five failure categories collapse to one generic routing reason; the waits map 1:1. (2) `BlockedDetails` **reshaped to typed keys** `{reason, category, sub_phase, needed}` (Farzam's pick), legacy outreach fields dropped. (3) The `pr-manager` **PR-creation cluster deleted now, rebuilt in S7** (Farzam's pick); `push` absorbed commit-and-push; `removeThoughtsAndPush` stays. (4) **Held at `push`** per the S4 flag. (5) The runner **persists task position live** (dark harnesses got a no-op `updateTaskField`) and **resume re-runs the checkpointed sub-phase** (re-applying its route reproduces the flow). (6) **Outreach delivery kept** and wired into the `awaiting_human` block path; the no-contacts escape moved into the gather prompt.

**Session 6 decisions (see `sessions/45.md`):** (1) **Plugin reports facts, Core owns policy** (Farzam-aligned, D8-faithful): the plugin's `pr_ready_to_merge` is formal approval + CI-green + mergeable; `/approve` recognition + authorization live in Core (`pipeline/pr-events.ts`), so a new hosting plugin never learns the `/approve` convention or sees the people-directory. (2) **`/approve` is a real approval path, equal to a formal approval** (Farzam's steer): a sole contributor can't self-approve on the host, so `/approve` counts; **authorization is permissive when no people are configured, gated once an owner/reviewer is** (public-repo self-merge floor — Trust Through Restraint), owner-only tightening a future knob; documented in `constraints.md`. (3) **Thin payloads** (Farzam's steer): only `pr_comments` carries data (the comments, for dedup + finding `/approve`); richer CI/conflict detail is fetched live via the plugin, not stored on the task. (4) **`entryFor`/`arbitrate` moved** from `pipeline.ts` into the new cohesive `pipeline/pr-events.ts`; `pipeline.ts` stays the PIPELINE map.

**Session 7a decisions (see `sessions/46.md`):** (1) **`pending_pr_event` stores the event TYPE, not the full payload** (a refinement of the approved new-column choice): `entryFor` only routes on type, the four thin events *are* their type, and a comments event's content rides `review.feedback_rounds`; this is S6-thin-faithful and avoids a `task.ts`→`adapters` import cycle — the `PrEventType` enum moved to a dependency-free leaf `schemas/git-hosting-event-types.ts`. (2) **Rework context reaches the re-entered agent via the runner's `carry`** (`ResumeState` gained `carry`), seeded by `reentryCarry(type, task)` — `gather`/`implement`/`design` already render it, so no new prompt plumbing. (3) **7a handles rework events only** — the merge-class events (`pr_ready_to_merge`/`pr_merged`) and the `/approve`-to-merge promotion are deferred to 7b, so a poll never spuriously reworks on an approval and nothing completes-without-merging while `auto-merge` is still shelled. (4) **`/approve` suppression in 7a**: an authorized `/approve` (and a bare changes-requested with no comment text) is dropped from the actionable set — the task waits, it does not rework. (5) **`create-pr` rebuilt** (new-PR + rework paths): composes title/body, opens the PR, persists `review.pr_number`; on rework dismisses the stale approval + marks feedback applied. (6) **`review-handler` → `pr-event-poller`**: stateless (the `approvedAwaitingCI` map is gone), circuit breaker preserved; the `task.feedback_received` + `review.poll_completed` events deleted with it.

**Next session's task (Session 7b of implementation — overall Session 47):** Live auto-merge + consumer audit. (a) **`/approve` promotion + merge routing** in the poller: an authorized `/approve` with a green/mergeable re-check promotes to `pr_ready_to_merge` (dropping the same-poll `pr_comments`); route `pr_ready_to_merge`/`pr_merged` → `auto-merge`. (b) **Live `auto-merge` body**: remove-thoughts-before-merge + safety-gated `mergePR` + remote-branch deletion + `git.branch_deleted`; `pr_merged` detects already-merged → done; a merge failure loops back via `pr_ci_failure`/`pr_merge_conflict` counted by `total_reworks` (D12). (c) **Consumer audit (D11)**: `unblock-resolver` + `response-poller` must not unblock a `pr_review_pending` task on a human comment (those resume via PR events) — health-monitor already filters it, so verify + add a test. See plan § "Session 7" (the 7b half).

**Deferred (do in the session that consumes them):**
- ✅ **DONE Session 2:** the unified `BlockCategory` + `BlockDetail = { category, sub_phase, needed }`.
- ✅ **DONE Session 5:** `BlockReason` derivation (`pipeline_failed` rename + the `BlockCategory`→`BlockReason` map at the wiring boundary); `agentStep` bookkeeping (safety gating, `cost.incurred` with the real plugin id, usage tracking) + the self-unblock cost emission; the phase rename + the `sub_phase`/`phase_iteration`/`total_reworks` typed columns on tasks AND checkpoints (runner persists + resume restores); the bounded build-dark duplication (legacy `agent-runner`/`session-result` deleted; `rrpir.review_phases` deleted; the skill map rekeyed to the new phase names; dead ComponentTags removed); and **`push`'s live body** (push-only delivers end to end).
- ✅ **DONE Session 6:** `GitHostingAdapter.detectPrEvents` contract + the per-variant `PrEvent` payloads; github-hosting `doDetectPrEvents` (pure `derivePrEvents`, stateless `pr_ready_to_merge`); Core `dedupePrEvents` + `findAuthorizedApproval` in `pipeline/pr-events.ts` (with `entryFor`/`arbitrate` moved there); fake plugin + contract suite gained `detectPrEvents`.
- ✅ **DONE Session 7a:** the `pending_pr_event` task field (the event type) + `executeTask` re-entry (`entryFor`→cursor, counters reset, `carry` seeded, consume-once); `create-pr` rebuilt (open + rework); `review-handler` replaced by the stateless `pr-event-poller` (rework events only); `task.feedback_received`/`review.poll_completed` events deleted; `await-review` re-entry comment resolved.
- **auto-merge still shelled — Session 7b:** `delivery/auto-merge` owns real routing/skip-gates/observability but its `mergePR` side-effect carries `TODO(farzam)`. 7b fills the live merge body, adds the `/approve` promotion + `pr_ready_to_merge`/`pr_merged` routing in the poller, and does the `blocked(pr_review_pending)` consumer audit (unblock-resolver + response-poller).
- **Stale name/shape in docs:** the slice file (`slices/08-pipeline-phases.md`) still says `cli-run` and the old `BlockDetail` shape; reconciles in the **Session 9 project-wide docs sweep** (which also fixes README's "seven phases … integration" lag and the live `docs/user-flows/pr-management/overview.md`, whose table still points at the deleted `review-handler.ts` for review-polling/merge — the rewrite waits on 7b's merge). Divergences to reconcile there: the lens-factory vs the "copy `security.ts`" prose; `refine`'s 5-verdict vocabulary; `review.lenses`; and the now-vestigial `PhasesConfig`/`DemoConfig` (no `src/` reader after the cutover) + the defensive `error` `ExecuteTaskResult` variant. The **dashboard's legacy phase names** (`self_review`/`demo_prep`) belong to **Slice 13** (all dashboard UI). Code is truth meanwhile.
- **`verify` runs gates synchronously** (`execFileSync`), blocking the daemon's event loop while a gate runs — fine at single-task concurrency. Captured in `docs/future-considerations.md` (Asynchronous Verification Gates); revisit when the daemon runs tasks concurrently.

## Completed Slices

- **Slice 1: Standards Alignment.** `docs/coding-standards.md` written. 10 categories decided via deep Q&A.
- **Slice 2: Repo Readiness.** Biome aligned, lint split, CI parallelized, tests restructured (`tests/unit/` mirrors `src/`), migrations consolidated, hardcoded paths fixed.
- **Slice 3: Dashboard.** 5-page React SPA rewrite (Overview, Tasks, Activity, Metrics, Errors), all features working, coding standards audited. Sessions 4-8, detail in `slices/03-dashboard.md`.
- **Slice 4: Startup & Configuration.** Getting-started path (`pnpm run setup` -> `engineer start`), OS detection gate, seed-example sanitization + dogfooding, removals (checkCliArtifacts, config-version machinery, Output.table, quiet mode), CLI restructure (Screaming Architecture), original coding standards audit (1-11), new coding standards added (sec 4 expanded, sec 5 expanded, sec 7 framing, sec 12-15), six post-bootstrap infrastructure gaps closed (retryable flag, cause chains, trace_id correlation, floating promises, span/log correlation, graceful degradation logs), and new standards applied across slice 4 in-scope files. "Apply with judgment, never mechanically" principle codified. Sessions 9-16, detail in `slices/04-startup.md`.
- **Slice 5: Trigger & Requirements (Contacts) Flow.** PluginContext + per-plugin StateStore (the SDK foundation), durable dedup moved to Core on `idempotency_key`, dead `trigger.pr_review` scaffolding removed (issues-only trigger), per-plugin poll cadence + configurable label/assignee work selection + Core-owned backoff, single-user constraint (`docs/constraints.md`, owner assumed-not-required, doctor "People Directory" category). Closing standards sweep (Session 22) closed it: deleted unwired config hot-reload infra, re-synced bundled plugin docs with source, removed dead `max_tokens`, fixed the chronic orchestrator test flake, line-by-line audit of all in-scope files. Sessions 17-22, detail in `slices/05-trigger.md`.
- **Slice 6: Scheduling & Dispatch.** Decomposition consumer deleted in full, single retry-policy module (per-category, config-driven), dispatch-tracker primitive (AbortController, per-dispatch identity, idempotent late callbacks), `Outcomes.terminated` with typed reason routing, preemption tightened (eligible filter, bounded priority, one-per-tick), hard-cap enforcement (`max_active_duration_ms` -> terminate -> failed + alert), crash recovery unification (boot-loop hole closed), `engineer retry` accepts failed tasks, `failed -> queued` transition, dead `preemption.ready` event deleted. Closing standards sweep (Session 29) fixed stale overview States table, dispatch-tracker drain complexity, Array<T> syntax, swallowed DB-open error in retry CLI, stale decomposition JSDoc. Post-sweep refinement (Session 30) collapsed triplicated LLM subprocess helpers (env allowlist + buildAgentEnv + appendStderr) into `src/plugins/agent/subprocess.ts`. Surfaced a new sweep principle: duplicated *functions* count under sec 11 too, not just literals. Sessions 25-30, detail in `slices/06-scheduling.md`.
- **Slice 7: Workspace & Session.** Five sessions, four implementation + closing sweep. Net -1568 lines across 79 files. Sessions 31-36, detail in `slices/07-workspace-session.md`.
  - **Session 31:** RRPIR for the slice (requirements + research + plan).
  - **Session 32:** Knowledge layer cut in full (KnowledgeStore, schemas, table, Dispatch.knowledge field, scheduler reads, all six prompt builders' knowledge plumbing, buildKnowledgeSection). Future-considerations entry captures the cross-task memory pivot. Commits `7893cf9` + `a36a213`.
  - **Session 33:** Session-memory hygiene + facade -> namespace. Enums trimmed (SessionEndReason 7 -> 5, JournalEntryType 7 -> 3, CheckpointReason 4 -> 2), dead columns dropped (`previous_session_id`, `resumed_from_checkpoint`, four journal type-specific columns), `JournalQueryFilters` dynamic SQL replaced with single static prepared statement, `getSessionChain` deleted, `ISessionMemory` interface deleted. SessionMemory now exposes `sessions`, `journal`, `checkpoints` as public readonly fields. 38 files, -479 lines. Commit `876b97a`.
  - **Session 34:** Workspace-manager extractions. `core/session-result/` extracted from orchestrator as a peer module, `core/skills/` created with `SkillsManager` class, `removeThoughtsAndPush` moved to pr-manager as a standalone exported function. `injectAuth` re-export + dead `push` branch cut. SSOT refinement: `workspace_root` tilde expansion hoisted into `system.ts`. 25 files, -270 lines. Commit `85afd55`.
  - **Session 35:** Workspace-manager state simplification. In-memory `workspaces` Map killed (DB is the single source of truth via `taskEngine.getTask(taskId)?.workspace`). `registerExistingWorkspace` deleted (nothing to register). `TaskWorkspaceSchema` gained `base_branch: string` (non-nullable, fixes the silent wrong-base-on-restart bug). `baseCommit` dropped from `WorkspaceRecord`. WorkspaceManager constructor takes a `WorkspaceManagerDeps` options object. `createWorkspace` owns the persistence write atomically with worktree creation. Commits `98b70bf` + `f7b4923`.
  - **Session 36 (closing sweep):** Four sweep commits, each green. (1) Cut dead schema surface from `schemas/ephemeral.ts`: 18 unused schemas/types had zero src/ consumers, several literally described the killed in-memory Map, one (`PendingPreemption`) was a dual-source-of-truth with a different-shaped runtime interface. Net -578 lines. Plus un-exported `recordFromTask` in workspace-manager. (2) Deleted backward-compat re-exports: `PHASE_SEQUENCE` re-export in `phase-runner.ts` + `isSlotConsuming`/`shouldPreempt` re-exports in `daemon/index.ts`; tests updated to import from real sources. Universal-rule violation closed. (3) Stale residue cleanup across comments, JSDoc, and live docs: `workspace-lifecycle` JSDoc fixed (re-register doesn't exist), pr-manager TODO got `(farzam)` per coding-standards sec 8, `phase-handlers` "all 7 phase handlers" stale count rewritten + three stale `(Session 0XX)` section dividers consolidated, four live docs cut "knowledge" mentions (overview, three-tier-model, zod-schemas). (4) Coding-standards compliance: three swallowed catches logged (Fail Loud), error message dropped redundant `WorkspaceManager:` prefix + quoted path (Deno style), `removeThoughtsAndPush` uses `WorkspaceNotFoundError` domain class instead of bare `Error`, `Array<{...}>` -> `{...}[]` shorthand. All four commits rewritten via cherry-pick + backup branch after Farzam called out em dashes (memory captured as `feedback_no_em_dashes_in_commits.md`). Final triad: 2357 unit + 39 integration + 16 e2e all green, lint clean (8 pre-existing complexity warnings unchanged, all outside Slice 7 scope), typecheck clean, build OK.
- **E2E test fix (carried over from Slice 5, Session 23):** the five `task-happy-path` + `crash-recovery` e2e failures were structural: tests used `clone_url: ""` (silently skipping workspace creation -> `WorkspaceNotReadyError` -> no LLM call), and asserted impossible LLM counts since the FakeLLM doesn't simulate the CLI's `session-result.json` write. Rewrote as one full-pipeline smoke test (bare git repo + FakeLLM side-effect hook) plus honest dispatch/routing/recovery tests. 16/16 e2e passing. Commit `f54ad8c`.
