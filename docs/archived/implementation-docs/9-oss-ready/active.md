# Active: Phase 9 (OSS Ready)

> **ALWAYS READ BEFORE PROCEEDING.** Then read [approach.md](approach.md) and the current slice file.
> These references are permanent. Never remove them.

## Key Files

- [vision.md](vision.md): why we're doing this, what done looks like
- [approach.md](approach.md): strategy, lenses, co-founder rules, RRP discipline (what to hunt for, how to present findings), closing sweep principles, 16-slice roadmap, session protocol
- Current slice: **Slice 9 (Completion & Cleanup / the reaper) — RRP COMPLETE (Session 51); implementation not started.** Design record: `slices/09-completion-cleanup.md`; build script: `.claude/temp/create-plan/slice-09-completion-cleanup.md`. Slice 8 (Pipeline Phases) is COMPLETE and merged to `main`.

## How This File Works

This file answers one question: **where are we right now?** Nothing more.

- **Current** holds the active slice: its state, plan, and the immediate next step.
- When a slice finishes, recap it as **one line** under **Completed Slices**, then advance
  **Current** to the next slice.
- Depth lives elsewhere: per-session detail in `sessions/N.md`, per-slice decisions in that
  slice's file. Do not duplicate that depth here.

## Current

**Slice 9 (Completion & Cleanup / the reaper) is COMPLETE and merged to `main`** — implementation `71ee80b` (state-model spine) → `f4263e5` (reaper + branch lifecycle + auto-merge) → `96ea542` (cancel end-to-end), and the closing standards sweep `92f68cc` (Session 55). Recap in **Completed Slices**; durable design record **`slices/09-completion-cleanup.md`** (read it first for the slice). **Pre-v1 run note still stands: delete `~/.engineer/data/engineer.db` before running (or `rm -rf ~/.engineer` for a full reset) — Slice 9 changed the schema + config, so the DB wipe is the migration.**

**Slice 10 (Communication) is BUILT, AUDITED, AND OWNER-REVIEWED — merged to `s10-continued` (Session 56).** RRP footwork done (Session 56): `.claude/temp/{requirements-gathering,research,create-plan}/slice-10-communication.md`. Durable design record: **`slices/10-communication.md`** (read it first for the slice). **Scope was narrowed to refine-over-build:** the notification digest, batching, quiet-hours, milestone/verbose phase-pings, and question_batching are **CUT** (deleting their dead config is the refinement); the one new capability is decision-escalation. Session order: S1 (router refinement, foundational) → S2 (cancel/label) → S3 (decision engine) → S4 (ask round-trip) → S5 (inbound query) → S6 (AUDIT-1 code) → S7 (AUDIT-2 docs). All seven done; all gates green (2542 unit + 64 integration + 16 e2e, lint 0 warnings). **DB wipe is the migration** (cut config + moved suppress window): delete `~/.engineer/data/engineer.db` before running.

> **Owner final review (Session 56) — DONE.** Independently re-ran every gate (lint 0 warnings, typecheck clean, 2538 unit + 64 integration + 16 e2e) and read the load-bearing code (router suppression/observability/retry, the escalation consult, the reaper cancel, the config cuts). **Deleted** the unwired per-person `quiet_hours`/`notification_level` scaffolding (`PersonSchema.preferences`) left dead by the cut policy — schema (incl. `NotificationLevelSchema`) + `setup.ts` + both people templates + `people.md` + the test fixtures (the S7-flagged item 1, resolved). Fixed the suppress-window wording nit (`type` → `kind`). **Endorsed the alert-dedup decision** (note below). Still open and explicitly NOT Slice 10 (pre-existing): the `AGENT_README` bundled-mirror drift in `cli/bundled/plugin-docs.ts` — a dedicated agent-docs mirror sync for a future session. Merged `slice10-build` → `s10-continued`.

- **S1 (Session 57) DONE** — notification-router refinement. Made the outbound path observable (deliver/send-fail/retry-success/retry-exhausted each emit a `tool_execution` observation alongside the existing `comm.*` events; a suppressed duplicate records a `decision_point`). Unified duplicate suppression: deleted the daemon's hardcoded `HEALTH_NOTIFY_COOLDOWN_MS` + `healthNotifyCooldowns` map + its tick cleanup; the router now owns one suppress window (`DaemonConfig.notification_suppress_window_ms`, default 5m) keyed on `(kind, taskId | alert source)`, and the 5 daemon health alerts + the reaper-failure alert route through it (alerts carry a stable `source`, so null-task alerts still dedup per origin). Cut the dead `OrchestratorConfig.notification` (digest/batch/quiet_hours/milestone_based) + `question_batching` schemas, their YAML templates, and the orchestrator.md sections — `OrchestratorConfig` is now `{review, observability}`. `review_reminder` resolves to the **owner** (single-user). Fixed the untyped `system.health_changed` publish (typed event + payload schema + daemon EVENTS declaration). Gates green (lint 0 errors, typecheck clean, 2510 unit + 64 integration + 16 e2e). Detail in `sessions/57-slice10-s1.md` (the bare `sessions/57.md` is the unrelated Dashboard Sync tangent S1 — the Slice-10 build reused the session counter on its own branch).
- **S2 (Session 58) DONE** — cancel/label coverage, reaper-driven. The reaper (`workspace-reaper/index.ts`) is now the **single emitter** for the cancel courtesy comment + label across every cancel path. `reapCancelledTask` calls a new `announceCancel(task)` before any reap step or early return: it `notify`s the `ticket_comment` "Task cancelled by the owner." and calls `notifications.syncStateToCommPlugin(...)` directly (NOT a synthetic `task.state_changed`, so no other state subscribers wake) so `engineer:cancelled` is applied — `diffStateLabels` is fully dynamic, no label-set change. This fixes the gap where a **non-active** cancel (queued/blocked via CLI or dashboard) got no comment and kept a stale label forever (those cancels are raw DB writes emitting no `task.state_changed`, and cancelled is terminal so it never self-corrected). The comment + sync are best-effort with **isolated try/catch** (one failing never skips the other, neither blocks `markReaped`), gated by an in-memory `cancelAnnounced` first-visit guard so a retrying reap (e.g. PR-close fails → `reaped_at` stays NULL → revisited) does not re-comment each sweep (the guard clears on a successful reap). A `state_transition` observation (`cancel_reconciled`, `trigger: cross_process_cancel`) makes the dashboard show *why* the label + comment appeared. Removed the now-duplicate active-cancel `ticket_comment` from `task-scheduler.ts` `handleUserCancelledTermination` (kept its `observer.info` abort record). `engineer retry` self-heals its label on the next dispatch's `queued→active.working` transition (that goes through `requestTransition`, which publishes `task.state_changed` → `daemon:state-sync` → `syncStateToCommPlugin`) — **no retry hook added**, confirmed. `docs/usage-guide/writing-tickets.md` gained the `engineer:cancelled` label row (no bundled mirror lists the label set). Gates green (lint 0 errors / 2 pre-existing router complexity warnings, typecheck clean, 2514 unit + 64 integration + 16 e2e). Detail in `sessions/58-slice10-s2.md`.
- **S3 (Session 59) DONE** — decision-escalation engine + policy wiring (**the one new BUILD capability**). The built-but-dead `should_i_ask` autonomy path is now wired end-to-end as an **enforced engine** (agent surfaces, runner enforces, `next()` stays pure). A generic `DecisionsSchema` (`pipeline/types.ts`, array of `{category, summary, chosen, reasoning, details?}`) is validated centrally in `agent-step.mapResult` so ANY phase can surface `details.decisions` (a malformed entry fails loud as `details_invalid`). The **runner**, after `emitSubPhaseResult` and before routing, consults `ctx.safetyLayer.consultJudgment({type:"should_i_ask", …})` per surfaced decision (new `consultDecisions` effect helper); the first non-`proceed` verdict becomes an `awaiting_human` block carrying a synthesized question, otherwise routing continues. `consultJudgment` now threads the **full `traceScope`** (contract change: `SafetyQuery` gained `trace?: SpanOptions` on the interface + input-validation schema), so the `autonomy_policy` decision nests in the dispatch trace instead of orphaning under a bare `task_id`. The shared **system prompt** (`agent-prompt.ts`) teaches the agent to surface decisions + the category vocabulary (engine enforces, prompt informs — reaches every agent sub-phase by construction). **Curated autonomy defaults** ship as the schema default (`DEFAULT_AUTONOMY_DECISIONS` in `config.ts` — zero-config gets sensible behavior, not "ask on everything") and are mirrored in both safety templates: `always_decide` = code_style/test_coverage/refactoring_local/doc_wording; `threshold (files > 5)` = scope_expansion/refactoring_broad; `always_ask` = architecture/dependencies/public_api/destructive/security; unknown → always_ask (fail-safe). **Threshold-gap fix:** `evaluateThreshold` now returns a 3-state `ThresholdOutcome` (`exceeded`/`within`/`metric_absent`); an absent metric routes to `ask_human` (was: silently proceed). **SafetyQuery cleanup:** deleted the dead `schemas/orchestrator.ts` schemas (`SafetyQuerySchema`/`SafetyVerdictSchema`/`CommEventSchema`/`QuestionSchema`/`QuestionBatchSchema`) + their test — `SafetyQuery`/`SafetyVerdict` single-sourced on the interface. `docs/configuration/safety.md` documents the categories + the surface-vs-hard-block distinction; README safety section reflects autonomy escalation is now enforced. Gates green (lint 0 errors / 3 pre-existing router complexity warnings, typecheck clean, 2516 unit + 64 integration + 16 e2e, build OK). Detail in `sessions/59-slice10-s3.md`.
- **S4 (Session 60) DONE** — ask round-trip refinement (Pillar B). The answer-injection RETURN path was already done on main (`68a7`: `pending_response` → `resolveDispatchStart` → `resolveResponse` → `responseCarry` carries the owner's reply into the re-run) — verified for a non-requirements phase, not rebuilt. Built the three remaining gaps. **(1) Generalize forward outreach beyond requirements:** every agent sub-phase now exposes `SubPhase.resultDir` (single-sourced with its `agentStep.directory`), and `deliverOutreach` resolves the blocking sub-phase's `outreach/` directory from it (new exported `outreachDirForSubPhase`) instead of hardcoding `requirements/outreach` — so a `needs_human` from research/planning/execution/review (incl. the nested `review/<lens>` layout)/delivery delivers its files; when the asking sub-phase wrote no file (the autonomy escalation, whose question is synthesized into `detail.needed`), `deliverNeededToOwner` delivers the question to the owner directly so it is never lost. **(2) No-owner edge:** an escalated autonomy verdict with no owner configured no longer blocks (which would strand the task) — the runner proceeds and records a loud warn-level, confidence-0.5 `autonomy_no_owner` `decision_point` naming the call made without the owner; the `getOwner()` check lives in the RUNNER, the safety layer stays owner-agnostic. **(3) Self-unblock exemption:** a new `BlockCategory` `awaiting_human_decision` (distinct from `awaiting_human` = stuck/needs-info; both still route to coarse `need_more_info`) marks a discretionary decision the owner must confirm; the daemon's `evaluate_self_unblock` stage skips it (only the owner can decide), while reminders + final escalation still fire. Mirrored into the dashboard taxonomy (`vocabulary.ts`/`constants.ts`). Gates green (lint 0 errors / 3 pre-existing router complexity warnings, typecheck clean, 2529 unit + 64 integration + 16 e2e, build OK). Detail in `sessions/60-slice10-s4.md`.
- **S5 (Session 61) DONE** — inbound query routing fix + enrichment (Pillar C). `handleQuery` (status/progress/cost/help) was unreachable: the response-poller linked every inbound message to a task (metadata or the sole-blocked fallback) before it could be seen as a query, so a Telegram "status" while one task was blocked was mis-routed as an unblock reply. The poller now **classifies first** (`classifyInbound`, a pure fn): metadata link → reply; **query vocabulary → query and WINS over the sole-blocked reply** (locked gate decision — the owner can ask `status` mid-block); else sole-blocked → reply; 0-or-2+-blocked → query. The poller calls `handleQuery` **directly** (still publishing a `task_id=null` audit `comm.message_received`) and the redundant `daemon:comm` subscription is **removed**, closing the latent external_ref double-dispatch. `ResponsePollerContext` widened with `safetyLayer`/`notifications`/`peopleDirectory`. `handleQuery` resolves the response recipient to the **owner** explicitly (was: raw sender → accidental getOwner fallback); no owner → warn + no reply. Formatters enriched (active+blocked tasks by id+title, `blocked.reason`, the cost verdict + percent-of-limit warnings, help enumerates the forms) while staying short. A 2+-blocked unmatched message becomes an owner-facing "couldn't match — N blocked" notice (was a silent discard). Observability: `inbound_route` `decision_point` (the classification, invisible until now) + an `inbound_query_handled` `lifecycle` per served query. Stale `daemon:comm` topology comment corrected. Docs: comm README "Inbound queries" section + telegram note (+ bundled mirror, parity-diffed); `future-considerations.md` Smart Reply Correlation corrected (the `[Task: …]` tag is a breadcrumb, NOT parsed on receive — correlation is structural). **Cost note:** surfaced the existing `cost_check` verdict + warnings rather than adding a `getCostStatus` method (the limits aren't on `CostStatus`, so a new method wouldn't show "vs limit" any better — minimal footprint). Gates green (lint 0 errors / 3 pre-existing router complexity warnings, typecheck clean, 2542 unit + 64 integration + 16 e2e, build OK). Detail in `sessions/61-slice10-s5.md`.
- **S6 (Session 62) DONE** — AUDIT-1, the slice-closing code+tests standards sweep. Read every Slice-10-touched source + test file (S1–S5) full-file against `coding-standards.md`, `anti-patterns.md`, and the three observability tests. The work was overwhelmingly clean — the per-session closing discipline held. One MUST-fix: the daemon's only lint warnings were three cognitive-complexity violations in the rebuilt `notification-router.ts`, which the gate forbids (every prior session noted them as "pre-existing, out of scope" — the closing sweep is where they belong). Split the two functions along their seams (no rule suppression): `processRetries` → a pure `decideRetryFate` eviction-policy table + `emitRetrySucceeded`; the retry redelivery → an `inFlight`/error shell (`attemptRetryDelivery`) over a plain `redeliverEntry`; `sendToFirstReachable` → lift the all-failed branch into `handleAllContactsFailed`. **Every emission preserved verbatim** (4 `comm.*` events, 4 `tool_execution` observations, the suppress decision) so the dashboard's outbound trail is unchanged. Two consistency fixes: moved a stray mid-file `INotificationRouter` import to the top (newspaper order), and switched the daemon's 4 health-alert `notify` calls from the raw `"alert"` literal to `NotificationKinds.alert` (the constant-over-string convention the rest of the daemon follows). Observability verdict: the new `awaiting_human_decision` block category is mirrored into both dashboard client files, the autonomy/route/suppress/cancel decisions all record richly, and the three `lifecycle` usages (`task_picked_up`/`pipeline_completed`/`inbound_query_handled`) are genuine milestones, not overloads. Gates green (lint **0 warnings** — the 3 are gone, typecheck clean, 2542 unit + 64 integration + 16 e2e). Detail in `sessions/62-slice10-s6.md`.
- **S7 (Session 63) DONE** — AUDIT-2, the slice-closing docs + bundled-mirror sweep. Verified every Slice-10-touched doc against docs-as-blueprint + no-stale-counts, grep-confirming paths/config-keys/defaults/capability names against the code as it now is. **Diffed both hand-maintained bundled mirrors byte-for-byte against their live markdown** (extracted each template-literal block, unescaped, `diff`-ed). Confirmed: the cut config is fully gone from BOTH live docs and bundled templates (`OrchestratorConfig` = `{review, observability}`); the new behavior (suppression, autonomy categories, cancel label, inbound query) is documented in BOTH; the autonomy category vocabulary is synced across all four sources (`config.ts` `DEFAULT_AUTONOMY_DECISIONS`, `templates.ts` × both safety templates, `agent-prompt.ts`, `safety.md`). Fixed four mirror/template drifts (commit `9f79082`, docs-only): the "mirrored trees" coding-standards § 7 link missing from all three adapter-README mirrors (pre-existing Slice 5); the github-comm "swallowed → logged at warn" line (pre-existing Slice 5); the suppress-window comment "type → kind" to match the code's `${kind}:${scope}` dedup key + `daemon.md` (S1-touched); and the `review` block added to both orchestrator templates (orchestrator.md documents `review.lenses` with an example, the templates omitted it — YAML-parse-verified). Flagged two out-of-scope items for the owner's final code sweep (see the note above): the unwired per-person `quiet_hours`/`notification_level` scaffolding, and the pre-existing `AGENT_README` mirror drift. Gates green (lint 0 warnings, typecheck clean, 2542 unit + 64 integration + 16 e2e). Durable slice design record written: `slices/10-communication.md`. Detail in `sessions/63.md`. **Slice 10 build complete — next is the owner's final review + merge.**

> **Note on the alert-bypass instruction (flagged for final review):** the S1 brief said both "route the 5 health alerts through [the suppress window] so they still dedup per-source" AND "ALERTS BYPASS suppression." Those contradict. Resolved as **alerts ARE deduped by the suppress window** — that is exactly what the deleted `HEALTH_NOTIFY_COOLDOWN_MS` did, the per-source `dedup_key` requirement is only meaningful if alerts dedup, and bypassing would reintroduce owner-flooding from a flapping trigger. The "bypass" wording was a leftover from the now-cut multi-stage policy (quiet-hours/batch/digest), where an alert must never be *delayed*. **Owner confirmed at final review: keep alerts deduped.**

**Deferred (do in the slice that consumes them):**
- **Slice 10 (Communication):** the reserved `OrchestratorConfig.notification.*` + `question_batching.*` policy knobs and the `DigestConfig.include` `cancelled` entry were all **CUT** by the Slice 10 create-plan (refine-over-build) and **deleted in S1** — they are no longer deferred. The cancel `ticket_comment` for non-active cancels, the `engineer:cancelled` comm-plugin label + cross-process label sync, and the `writing-tickets.md` row were **all done in S2 (Session 58)** — the reaper is now the single emitter for the cancel comment + label, covering every cancel path; `engineer retry`'s label self-heals on the next dispatch (no hook). The reaper-failure `alert` already routes correctly through the now-live S1 suppress window. **Nothing from the Slice 9 cancel/label handoff remains deferred.**
- **Slice 13 (Dashboard UI): DONE via the Dashboard Sync tangent (Sessions 57–61, merged to `main`).** All surfacing of the new shape landed — `phase`/`sub_phase`/`phase_iteration`/`total_reworks`, the simplified state machine, block-reason taxonomy, routing/skip/loop decision trails, legacy `self_review`/`demo_prep` cleanup, the `cancelled` state, `reaped_at`, and the reaper sweep summary (surfaced via a new durable `system.reap_completed` event, since `getLastRun` is cross-process-invisible). What remains under Slice 13 is the non-sync polish from `approach.md` item 15 (docs site / VitePress, demo mode, license, design-history archive) — not the engine-sync this tangent closed.

## Tangent: Dashboard Sync

Pulls the dashboard-sync + surface-everything portion of Slice 13 forward (spec: `tangent-dashboard-sync.md`). Runs on `tangent-dashboard-sync-build`; sequential sessions S1–S4, each green on every gate.
- **S1 (Session 57) DONE** — Vocabulary single-source + API truth (no new UI). Stood up the parity-tested client vocabulary module (`client/src/lib/vocabulary.ts` + `tests/unit/dashboard/vocabulary-parity.test.ts`); killed `self_review`/`demo_prep` across `src/dashboard` (6 real phases now); fixed `/tasks` `phases_ran` to derive distinct real phases from observation `input.phase`; added `sub_phase`/`phase_iteration`/`total_reworks` to the list columns + contract; fixed the `errors` API stale event types; typed `reaped_at`/full `blocked`/`pending_pr_event`/`links`/`last_trace_link` into the client contract. Detail in `sessions/57.md`.
- **S2 (Session 58) DONE** — Shared presentational components for the real emitted shapes. Rebuilt `phase-pipeline.tsx` (6 real phases, readable labels not first-letters, current/completed marking, optional sub-phase + iter/reworks line, `compact` mode for list rows); new `decision-card.tsx` (context + chosen-among-alternatives + reasoning + confidence meter), `blob-viewer.tsx` (lazy `/api/blob` text fetch on expand, copy, empty/404/error states), `verdict-badge.tsx` (`VerdictBadge` pill + `GateList` + `VerdictPanel`), `block-badges.tsx` (`BlockReasonBadge`/`BlockCategoryBadge`/`BlockBadges` with `needed` next-step). New pure readers `lib/observation-shapes.ts` (`readDecision`/`readVerdict`, malformed-safe) + `lib/blob.ts` (`fetchBlob`/`isBlobRef`), both unit-tested (`tests/unit/dashboard/{observation-shapes,blob}.test.ts`, +24 tests). Block reason/category label+color maps added to `constants.ts`. Components are presentational and not yet imported by any page (knip ignores `client/**`, so green) — wiring is S3. Detail in `sessions/58.md`.
- **S3 (Session 59) DONE** — Task detail rebuilt on the real engine shape, S2 components wired in. Overview surfaces sub_phase/iteration/reworks/`reaped_at` + the legible block taxonomy (BlockBadges with `needed`); deleted the `phasesFromDetail` hack (PhasePipeline now drives off real `phase` + observation-derived phases-ran); `isCancellable` matches CANCELLABLE_STATES (adds `requirements_gathering`). Phases tab rebuilt: groups phase_transition by `input.phase` in pipeline order with the sub-phase sequence + routing/skip/loop decision trail + per-phase cost/duration from `agent_call` output. Agent tab reads cost/tokens from `output` (never null `metadata`), wires BlobViewer for prompt/result/transcript, joins the model from `cost.incurred` events (no step-as-model mislabel). New Decisions tab (`?type=decision_point`, chronological DecisionCards) wired into TAB_ROUTES + routes + nav. Timeline renders decisions/verdicts/agent-calls in their rich forms with blob drill-down + dispatch boundaries. Tools tab cross-links gate verdicts. `blocked-response.tsx` uses BlockBadges. New readers `readAgentCall`/`readPhaseTransition` (observation-shapes) + `lib/cost-events.ts` (`modelsFromCostEvents`), unit-tested (+12 tests, 2398 total). Detail in `sessions/59.md`.
- **S4 (Session 60) DONE** — Lists/overview/activity sync + the reaper vertical + polish + docs (final build session). Reaper vertical: added the durable `system.reap_completed` event (schema mirroring `ReapStats`, registered in the reaper's EVENTS, published at the end of every `runOnce()`), so the dashboard's new overview **Cleanup card** (`/api/events?type=system.reap_completed`) can surface recent sweeps across the process boundary; per-task `reaped_at` was already on the detail overview from S3. Lists: task-table phase column now shows the active sub-phase + loop counters for in-flight tasks; task-filters gained the `cancelled` chip and a secondary block-reason filter (driven by a new `block_reason` enum + `reaped_at` surfaced on the list row from the `blocked` payload); the blocked overview card shows the real BlockReason badge. Activity already iterates every real ObservationType (no stale type). Polish: sidebar version single-sourced from `package.json` via Vite `define` (`__APP_VERSION__`), no hardcode. Docs: observability how-to "Dashboard Integration" + architecture dashboard section updated to the real view set (incl. the Decisions tab + Cleanup card), no stale counts. +6 tests (2404 total). Detail in `sessions/60.md`. **Tangent build complete.**
- **Final review + merge (Session 61) DONE** — orchestrator hands-on review: re-ran every gate independently (2405 unit + 67 integration + build green), read every diff, and ran the built dashboard against a seeded DB exercising every endpoint the UI consumes (real phases, agent cost from `output`, decisions with reasoning/confidence, the verify verdict, the reaper sweep, the model join, and blob drill-down all confirmed). Merged `main`'s mid-build `358aa09` (OTLP trace gating) into the branch cleanly. Final-gate fix: surfaced `scanned`/`skipped_in_flight` on the overview Cleanup card (`58bd49a`). Merged worktree → `main`. Detail in `sessions/61.md`.
- **Future hardening — `verify` runs gates synchronously** (`execFileSync`), fine at single-task concurrency. `docs/future-considerations.md` (Asynchronous Verification Gates).

## Completed Slices

- **Slice 1: Standards Alignment.** `docs/coding-standards.md` written. 10 categories decided via deep Q&A.
- **Slice 2: Repo Readiness.** Biome aligned, lint split, CI parallelized, tests restructured (`tests/unit/` mirrors `src/`), migrations consolidated, hardcoded paths fixed.
- **Slice 3: Dashboard.** 5-page React SPA rewrite (Overview, Tasks, Activity, Metrics, Errors), all features working, coding standards audited. Sessions 4-8, detail in `slices/03-dashboard.md`.
- **Slice 4: Startup & Configuration.** Getting-started path (`pnpm run setup` -> `engineer start`), OS detection gate, seed-example sanitization + dogfooding, removals (checkCliArtifacts, config-version machinery, Output.table, quiet mode), CLI restructure (Screaming Architecture), original coding standards audit (1-11), new coding standards added (sec 4 expanded, sec 5 expanded, sec 7 framing, sec 12-15), six post-bootstrap infrastructure gaps closed (retryable flag, cause chains, trace_id correlation, floating promises, span/log correlation, graceful degradation logs), and new standards applied across slice 4 in-scope files. "Apply with judgment, never mechanically" principle codified. Sessions 9-16, detail in `slices/04-startup.md`.
- **Slice 5: Trigger & Requirements (Contacts) Flow.** PluginContext + per-plugin StateStore (the SDK foundation), durable dedup moved to Core on `idempotency_key`, dead `trigger.pr_review` scaffolding removed (issues-only trigger), per-plugin poll cadence + configurable label/assignee work selection + Core-owned backoff, single-user constraint (`docs/constraints.md`, owner assumed-not-required, doctor "People Directory" category). Closing standards sweep (Session 22) closed it: deleted unwired config hot-reload infra, re-synced bundled plugin docs with source, removed dead `max_tokens`, fixed the chronic orchestrator test flake, line-by-line audit of all in-scope files. Sessions 17-22, detail in `slices/05-trigger.md`.
- **Slice 6: Scheduling & Dispatch.** Decomposition consumer deleted in full, single retry-policy module (per-category, config-driven), dispatch-tracker primitive (AbortController, per-dispatch identity, idempotent late callbacks), `Outcomes.terminated` with typed reason routing, preemption tightened (eligible filter, bounded priority, one-per-tick), hard-cap enforcement (`max_active_duration_ms` -> terminate -> failed + alert), crash recovery unification (boot-loop hole closed), `engineer retry` accepts failed tasks, `failed -> queued` transition, dead `preemption.ready` event deleted. Closing standards sweep (Session 29) fixed stale overview States table, dispatch-tracker drain complexity, Array<T> syntax, swallowed DB-open error in retry CLI, stale decomposition JSDoc. Post-sweep refinement (Session 30) collapsed triplicated LLM subprocess helpers (env allowlist + buildAgentEnv + appendStderr) into `src/plugins/agent/subprocess.ts`. Surfaced a new sweep principle: duplicated *functions* count under sec 11 too, not just literals. Sessions 25-30, detail in `slices/06-scheduling.md`.
- **Slice 7: Workspace & Session.** Five sessions, four implementation + closing sweep. Net -1568 lines across 79 files. Sessions 31-36, detail in `slices/07-workspace-session.md`.
  - **Session 31:** RRPIR for the slice (requirements + research + plan).
  - **Session 32:** Knowledge layer cut in full (KnowledgeStore, schemas, table, Dispatch.knowledge field, scheduler reads, all six prompt builders' knowledge plumbing, buildKnowledgeSection). Future-considerations entry captures the cross-task memory pivot. Commits `7893cf9` + `a36a213`.
  - **Session 33:** Session-memory hygiene + facade -> namespace. Enums trimmed (SessionEndReason 7 -> 5, JournalEntryType 7 -> 3, CheckpointReason 4 -> 2), dead columns dropped (`previous_session_id`, `resumed_from_checkpoint`, four journal type-specific columns), `JournalQueryFilters` dynamic SQL replaced with single static prepared statement, `getSessionChain` deleted, `ISessionMemory` interface deleted. SessionMemory now exposes `sessions`, `journal`, `checkpoints` as public readonly fields. 38 files, -479 lines. Commit `876b97a`.
  - **Session 34:** Workspace-manager extractions. `core/session-result/` extracted from orchestrator as a peer module, `core/skills/` created with `SkillsManager` class, `removeThoughtsAndPush` moved to pr-manager as a standalone exported function. `injectAuth` re-export + dead `push` branch cut. SSOT refinement: `workspace_root` tilde expansion hoisted into `system.ts`. 25 files, -270 lines. Commit `85afd55`.
  - **Session 35:** Workspace-manager state simplification. In-memory `workspaces` Map killed (DB is the single source of truth via `taskEngine.getTask(taskId)?.workspace`). `registerExistingWorkspace` deleted (nothing to register). `TaskWorkspaceSchema` gained `base_branch: string` (non-nullable, fixes the silent wrong-base-on-restart bug). `baseCommit` dropped from `WorkspaceRecord`. WorkspaceManager constructor takes a `WorkspaceManagerDeps` options object. `createWorkspace` owns the persistence write atomically with worktree creation. Commits `98b70bf` + `f7b4923`.
  - **Session 36 (closing sweep):** Four sweep commits, each green. (1) Cut dead schema surface from `schemas/ephemeral.ts`: 18 unused schemas/types had zero src/ consumers, several literally described the killed in-memory Map, one (`PendingPreemption`) was a dual-source-of-truth with a different-shaped runtime interface. Net -578 lines. Plus un-exported `recordFromTask` in workspace-manager. (2) Deleted backward-compat re-exports: `PHASE_SEQUENCE` re-export in `phase-runner.ts` + `isSlotConsuming`/`shouldPreempt` re-exports in `daemon/index.ts`; tests updated to import from real sources. Universal-rule violation closed. (3) Stale residue cleanup across comments, JSDoc, and live docs: `workspace-lifecycle` JSDoc fixed (re-register doesn't exist), pr-manager TODO got `(farzam)` per coding-standards sec 8, `phase-handlers` "all 7 phase handlers" stale count rewritten + three stale `(Session 0XX)` section dividers consolidated, four live docs cut "knowledge" mentions (overview, three-tier-model, zod-schemas). (4) Coding-standards compliance: three swallowed catches logged (Fail Loud), error message dropped redundant `WorkspaceManager:` prefix + quoted path (Deno style), `removeThoughtsAndPush` uses `WorkspaceNotFoundError` domain class instead of bare `Error`, `Array<{...}>` -> `{...}[]` shorthand. All four commits rewritten via cherry-pick + backup branch after Farzam called out em dashes (memory captured as `feedback_no_em_dashes_in_commits.md`). Final triad: 2357 unit + 39 integration + 16 e2e all green, lint clean (8 pre-existing complexity warnings unchanged, all outside Slice 7 scope), typecheck clean, build OK.
- **Slice 8: Pipeline Phases.** The 7-phase hardcoded pipeline rebuilt as a 6-phase data-driven sub-phase architecture (folders = phases, files = sub-phases; each file owns `run` + `next`; a generic `runner`; `agent-step` the one defended boundary). Absorbed original Slices 9 + 10 (downstream renumber −2). RRPIR landed Sessions 38-39; ten implementation sessions (overall 40-50). Across the slice the legacy runner / phase-handlers / agent-runner / phase-navigator / review-handler / the `pr-manager` PR cluster / `session-result` were all deleted via build-dark-then-atomic-cutover. Detail in `slices/08-pipeline-phases.md` + `sessions/40.md`–`50.md`.
  - **Sessions 1-4 (40-43):** foundation cleanup, then the entire new pipeline built DARK — core + upstream phases + Review/Delivery shells + the typed `PrEvent` vocabulary — proven end-to-end against real WorkspaceManager + SessionMemory with only the agent faked.
  - **Session 5 (44):** the atomic cutover — `executeTask` → `runPipeline`; legacy deleted; `001_schema.sql` finalized (2-counter model + `sub_phase` columns on tasks AND checkpoints). DB wipe = the migration.
  - **Session 6 (45):** `GitHostingAdapter.detectPrEvents` + the thin per-variant `PrEvent` payloads; stateless `pr_ready_to_merge`; Core owns `/approve` authorization + arbitration (`pipeline/pr-events.ts`).
  - **Sessions 7a/7b (46-47):** external re-entry via DB re-queue + `entryFor`; the 996-line review-handler replaced by the stateless `pr-event-poller`; the live `auto-merge` body + `git.pr_merged`/`git.branch_deleted` audit events; merge-failure routing splits by cause.
  - **Session 8 (48):** dev-toolbox skill principles ported/relocated into the prompts + `coding-standards.md`.
  - **Session 9 (49):** project-wide docs sweep + bundled-mirror sync + a full legacy-code audit.
  - **Session 10 (50, closing sweep):** the slice's final quality gate — three real bugs fixed (`verify` hung-gate, `retry` NULL-id, dashboard Cancel 500), two more core bugs, the captured follow-ups cleared (incl. the corrected `terminated`-branch follow-up), SSOT factories (`agent-cost`, `skipIfTrivial`), the seven dead bus events deleted, forward-scaffolding kept + documented. Four fix commits on `slice8-s10-sweep`; 2188 unit + 47 integration + 16 e2e, lint 0, build green. Detail in `slices/08-pipeline-phases.md` § Closing Standards Sweep.
- **Slice 9: Completion & Cleanup (the reaper).** A daemon-resident reconciliation reaper owns the terminal-task cleanup that cannot happen inline: it deletes merged branches once `branch_retention_days` elapses (the sole branch deleter — `auto-merge` only records the merge + backfills external merges) and reaps cancelled tasks (close any open PR, then the branch), all-or-nothing via a `reaped_at` marker, behind a real failure envelope (re-entrancy guard, per-task isolation, plugin-absent degradation, consecutive-failure `alert`, `getLastRun`). Adds a first-class `cancelled` terminal state with effective cross-process cancel — `engineer cancel` + the dashboard write a guarded versioned UPDATE that joins the daemon CAS, and the tick SIGTERMs an in-flight cancelled dispatch — plus a `TERMINAL_STATES`/`isTerminal()` SSOT; cuts `CleanupConfig` + `delete_branch_after_merge`. Four sessions (52-55), closing sweep `92f68cc`. Detail in `slices/09-completion-cleanup.md` + `sessions/52.md`-`55.md`.
- **E2E test fix (carried over from Slice 5, Session 23):** the five `task-happy-path` + `crash-recovery` e2e failures were structural: tests used `clone_url: ""` (silently skipping workspace creation -> `WorkspaceNotReadyError` -> no LLM call), and asserted impossible LLM counts since the FakeLLM doesn't simulate the CLI's `session-result.json` write. Rewrote as one full-pipeline smoke test (bare git repo + FakeLLM side-effect hook) plus honest dispatch/routing/recovery tests. 16/16 e2e passing. Commit `f54ad8c`.
