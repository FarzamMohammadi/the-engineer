{
  "status": "needs_human",
  "summary": "Root cause confirmed (rework path regenerates the PR description but never calls updatePR to push it); blocking on owner for the description's presentation format and which rework causes should refresh it.",
  "details": {
    "complexity": "moderate",
    "verification": {
      "commands": [
        { "name": "typecheck", "command": "pnpm", "args": ["run", "typecheck"] },
        { "name": "lint", "command": "pnpm", "args": ["run", "lint"] },
        { "name": "test", "command": "pnpm", "args": ["test"] }
      ]
    }
  }
}
