{
  "status": "ok",
  "summary": "Owner answered both forks and widened scope: regenerate PR title AND body as one unified rewrite from the full diff on every re-push, pushing only when content actually changed. Intent is fully owner-expressed (acceptance criteria pre-stated); remaining items are delegated design decisions. Proceeding.",
  "details": {
    "complexity": "moderate",
    "verification": {
      "commands": [
        { "name": "typecheck", "command": "pnpm", "args": ["run", "typecheck"] },
        { "name": "lint", "command": "pnpm", "args": ["run", "lint"] },
        { "name": "test", "command": "pnpm", "args": ["test"] }
      ]
    }
  }
}
