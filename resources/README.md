# Resources

Static artifacts shipped with The Engineer and copied to `~/.engineer/` at startup.

These files are loaded into agent context at runtime to enhance capabilities during task phases. They are not compiled — they are resolved by path and read from disk.

## Structure

- `docs/` — Behavioral and operational guides embedded into phase prompts
