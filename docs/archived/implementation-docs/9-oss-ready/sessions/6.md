# Session 6 — Dashboard: Tasks (List + Immersive Detail)

**Date:** 2026-05-11
**Slice:** 03-dashboard
**Duration:** ~1 session

## What Was Done

### Dependencies
- Installed `@tanstack/react-table` v8.21.3
- Installed `@radix-ui/react-collapsible` v1.1.12
- Added both to knip `ignoreDependencies` (dashboard client excluded from knip tracing)

### New UI Primitives (4 files)
- `components/ui/table.tsx` — Table, TableHeader, TableBody, TableRow, TableHead, TableCell
- `components/ui/textarea.tsx` — styled textarea
- `components/ui/input.tsx` — styled input
- `components/ui/collapsible.tsx` — Radix collapsible re-export

### New Shared Component (1 file)
- `components/shared/json-viewer.tsx` — recursive expandable JSON viewer with color-coded primitives, used in LLM/tools/overview tabs

### Task List (3 files)
- `pages/tasks/tasks-page.tsx` — client-side state filtering, skeleton loading, empty states
- `pages/tasks/task-filters.tsx` — state filter chips with counts and dot colors
- `pages/tasks/task-table.tsx` — TanStack Table with sorting, columns: title, state badge, phase pipeline, cost, tokens, last activity. Click row → navigate to detail

### Task Detail (7 files)
- `pages/tasks/task-detail-page.tsx` — immersive view with back nav, header (title + state badge + phase pipeline), cancel button, tab routing via URL params
- `pages/tasks/task-overview-tab.tsx` — 4 info cards (details, cost/tokens, timestamps, last transition) + acceptance criteria, decisions, workspace, review, external ref (all via JsonViewer)
- `pages/tasks/task-timeline-tab.tsx` — vertical timeline with left line, kind-colored icons (event/journal/action), timestamp, expandable payloads
- `pages/tasks/task-phases-tab.tsx` — RRPIR phase cards with duration bar, cost, execution count
- `pages/tasks/task-llm-tab.tsx` — collapsible LLM call rows with model, tokens, cost, phase. Expandable input/output/metadata via JsonViewer
- `pages/tasks/task-tools-tab.tsx` — collapsible tool execution rows with status badge, duration, expandable input/output
- `pages/tasks/blocked-response.tsx` — amber card showing blocked reason/question/context, textarea for response, Cmd+Enter to send

### Hooks (expanded)
- `use-tasks.ts` — added `useTaskTimeline`, `useTaskPhases`, `useTaskLlmTraces`, `useTaskToolTraces`, `useRespondToTask` (mutation), `useCancelTask` (mutation)

### Types
- `types/api.ts` — added `TimelineItem` and `TimelineResponse` interfaces

### Router
- `app.tsx` — task detail now routes to `TaskDetailPage` instead of `TasksPage`

### Backend
- `POST /api/tasks/:id/cancel` — validates task is in cancellable state, transitions to failed, writes state_transition record
- `TaskRoutesDeps` now includes `writeDb` for write operations
- `server.ts` updated to pass `writeDb` to task routes

### Config Fixes
- `tsconfig.test.json` — added `src/dashboard/client` to exclude (was causing JSX errors in test type-check)
- `knip.json` — added `@radix-ui/react-collapsible` and `@tanstack/react-table` to ignoreDependencies
- Existing test file updated to pass `writeDb` to task routes deps

## Verification
- `pnpm build` — passes (tsdown + vite)
- `pnpm lint` — passes (biome + tsc + tsc test + knip + madge). Only pre-existing complexity warnings in other modules
- `pnpm test:unit` — 2547 tests pass, 106 files
- `pnpm dev:dashboard` — Vite dev server starts on :5173

## File Count
- 14 new files in `src/dashboard/client/` (4 UI, 1 shared, 9 task pages)
- 5 modified root/config files (package.json, pnpm-lock.yaml, knip.json, tsconfig.test.json, biome: no change)
- 3 modified source files (app.tsx, use-tasks.ts, types/api.ts)
- 2 modified backend files (tasks.ts, server.ts)
- 1 modified test file (api.test.ts)

## Decisions
- No `select` component needed — filter chips (badge buttons) are better UX for task state filtering
- `useAwait` lint: mutations use arrow returns instead of `async/await` since `apiFetch` already returns a Promise
- Timeline items keyed by `${kind}-${timestamp}-${id}` composite for uniqueness
- LlmTraceRow: extracted `parseLlmMeta` + `LlmTraceHeader` sub-component to stay under complexity threshold
- `tsconfig.test.json` exclusion was a gap from Session 5 — fixed now
