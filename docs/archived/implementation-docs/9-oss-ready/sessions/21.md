# Session 21 — 2026-05-21

## What Happened

Executed **Plan Session 4 — Contacts: single-user constraint (D9)**, the final session of Slice 5,
closing the slice. Wrote `docs/constraints.md` as the home for deliberate v1 scope narrowings,
documenting the single-user constraint (the human side is one person — the owner), and referenced it
from the always-read AGENT-README, the README, and philosophy.md. Added a pure
`inspectPeopleDirectory(people, availableChannels)` that returns typed warnings for the three
single-user health problems (no owner, more than one person, owner channel with no installed comm
plugin); the daemon logs them at startup and a new `engineer doctor` "People Directory" category
renders them on demand. Fixed a latent doctor bug (the owner check read a singular `role` field while
the schema uses `roles[]`, so it always reported "no owner") and stripped positional "Category N"
counts per no-stale-counts. All gates green (typecheck, lint, 2497 tests; +16 new).

Mid-session, Farzam corrected the framing: the owner contact is **assumed, not required** — a missing
owner is a warning (naming the consequence: cannot reach you when blocked, needs context, or needs a
decision during requirements gathering), never a hard failure. Severity is `warn` everywhere.

## Decisions Made

1. **Owner is assumed, not required.** Missing owner / no people configured is a `warn`, never a
   `fail`. The warning names the consequence. This is the deliberate fail-soft exception for optional
   contact infrastructure (saved to memory).
2. **Constraint framing: "single human user (the owner)."** `docs/constraints.md` headlines a
   person-count constraint, with the two non-relaxations (one user ≠ one task, one user ≠ one plugin).
3. **Pure inspect-fn + thin shells (FCIS), not an observer in `PeopleDirectory`.** One pure
   `inspectPeopleDirectory` is the single source of truth for people-config health; bootstrap logs the
   warnings, doctor renders them. `PeopleDirectory` stays a pure lookup table.
4. **One dedicated doctor "People Directory" category.** Consolidates owner-presence + single-user +
   owner-channel validation; the owner check moved out of "Config Files" and the role-vs-roles bug is
   fixed. Channels derive from enabled, send-capable built-in comm manifests (doctor sees bundled
   plugins; the daemon validates the live registry).
5. **Channel validation runs at both startup and doctor** (per D9's "config load / doctor").

## Finding for Follow-Up

Config **hot-reload is unwired**: `createConfigWatcher`, `PeopleDirectory.updateConfig`, and
`SafetyLayer.updateConfig` are exercised only by tests — nothing instantiates the watcher in the
daemon, so no config hot-reloads at runtime. `configuration/README.md` still claims safety.yaml does
(500ms debounce). people.yaml docs corrected to "on restart" this session; the safety.yaml claim and
the dead watcher/`updateConfig` infra were left for a deliberate decision: wire it into bootstrap, or
delete and correct the docs.

## What's Next

**Correction (same session):** the feature work (Plan Sessions 1–4) is done and green, but Slice 5 is
**not** closed. Mirroring how Slice 4 ended, a closing **standards sweep** (Plan Session 5) remains:
a full audit/refine pass over every file Slice 5 created or changed across Sessions 17–21, against
`coding-standards.md`, `anti-patterns.md`, and `philosophy.md`. The inventory and tiering live in
`slices/05-trigger.md` → "Closing Standards Sweep". Begin there; close the slice only after the sweep
lands and all gates are green, then advance to Slice 6 (Scheduling & Dispatch). The flaky
`resolveStartState — feedback rework` orchestrator test still passes on isolated rerun.
