# Session 079 — 2026-03-27/28

## What Happened

Runtime Phase Refinement continued with Trigger & Requirements Flow (roadmap section 2 of 9). Deep codebase exploration of the full trigger-to-task pipeline: trigger poller, GitHub plugin, dedup mechanisms, task creation, requirements gathering phase, outreach flow, People Directory, communication plugins, response poller, and unblock resolver. Co-founder brainstorm across 6 groupings (trigger polling, dedup, task creation, requirements intelligence, outreach/communication, response detection). Expert panel review (5 panelists: Torvalds, Hipp, Pike, Engineer Persona, Technical Architect) applied — trimmed over-engineering, surfaced 7+ missed gaps, refined decisions.

Key panel convergence: plugin state = files not contract methods (unanimous), label priority = highest match wins (not first match), expertise tags deferred (4/5 said premature), response files = individual atomic files (not append), reply threading > reference codes for response correlation. Panel surfaced: blocked task timeout watchdog, issue closure as cancellation, rate limit as shared resource, concurrent unblock race condition, outreach dedup on restart, hallucinated person ID validation, `contacted` array always empty.

Final output: `ideation.md` (decisions + panel feedback + deferred items) and `research.md` (complete technical reference with exact line numbers, schemas, implementation ordering) in `8-refinement-v2/roadmap-ideas/trigger/`.

## Decisions Made

None formally numbered — brainstorm/ideation phase. Key directions agreed:

- ETags/If-Modified-Since for GitHub API polling (plugin-level optimization)
- Plugin returns structured ExternalRef (removes parseGitHubUrl from core — tier fix)
- DB-backed dedup via external_ref (tasks table = source of truth, in-memory = hot cache)
- Plugin persists own state as JSON files (no adapter contract change)
- Label-based priority mapping in config (highest match wins, `Math.max`)
- Preferred channel routing via People Directory resolveContact()
- Individual response files (atomic write-temp-then-rename, not append)
- Reply threading as primary response correlation (task reference as fallback)
- Block/proceed criteria as prompt guidance (not rigid rubric)
- Max loop → block + escalation notification to owner
- Extract sendOutreachFromFiles to own file (function, not class)
- Populate contacted array in BlockedDetails after outreach delivery
- Validate outreach person IDs against People Directory
- Blocked task timeout watchdog in daemon tick loop
- Issue closure detection as cancellation signal
- Rate limit proactive awareness (X-RateLimit-Remaining)
- Deferred: expertise tags, dedup observability, resume prompt enhancement, webhooks, PR review triggers, intake gate, schema-driven outreach, issue body sync

## What's Next

Planning session: build implementation plan from `8-refinement-v2/roadmap-ideas/trigger/ideation.md` + `research.md`. Plan goes to `trigger/plan.md`. Then execute the plan.
