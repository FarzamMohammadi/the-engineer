# Session 020 — 2026-03-08

## What Happened

Drafted Lifecycle Traces (`lifecycle.md`). Three end-to-end scenarios traced through all components, cross-referencing protocols.md, event-catalog.md, plugin-contracts.md, and error-propagation.md.

**Scenario 1: Happy Path** — "Add dark mode toggle to settings page." GitHub issue #47 through all 7 phases to merged PR. Exercises P1, P2, P3, P4 (all 7 phases), P7 (read + side-effect paths), P10 (normal cost tracking), P13 (milestones + GitHub state sync), P14 (status query mid-work). Includes fast-path variant callout.

**Scenario 2: Complex Path** — "Refactor auth module to support OAuth2." Decomposition into 2 children (A + B with dependency), preemption by urgent task #53, child A resume, merge conflict during progressive merge, sibling knowledge flow, parent integration. Exercises P2, P3, P4, P5, P6 (with conflict), P7 (ask_human verdict), P8, P9, P10, P11, P12, P13.

**Scenario 3: Resilience Path** — "Update API rate limiting middleware." Cascading failures: LLM failover (Chain 1), cost limit breach (P10), comm plugin failure during cost alert (Chain 3, Patterns 2+3), config hot-reload failure (Chain 7), Daemon crash and recovery (P15 Scenario B, Pattern 4), GitHub reconciliation, trigger failures (health alert), workspace recovery (Chain 6), Event Bus down/graceful-halt (Chain 2, Pattern 6), action pipeline rejection, checkpoint storage failure (Chain 4).

**Coverage verified:** 15/15 protocols, 30/30 events, all 5 plugin types + Registry + People Directory, all 7 error chains, all 6 recovery patterns. No gaps found.

## Decisions Made

No new architectural decisions. All design decisions for this document were presentational:
1. Fast-path as callout (not separate trace)
2. Single dense resilience scenario (not split)
3. Reference + data flow detail level (not fully self-contained or high-level only)

## What's Next

Mini holistic review — cross-reference all 5 Layer 3 docs against each other and against Layer 2 docs. Update any inconsistencies found. Then Layer 3 is complete.
