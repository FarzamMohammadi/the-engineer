/// <reference types="vite/client" />

/** The dashboard version, inlined by Vite from the root `package.json` (see `define` in `vite.config.ts`). */
// biome-ignore lint/style/useNamingConvention: the `__NAME__` form is the established Vite/bundler convention for a compile-time injected global, signalling "not a real variable".
declare const __APP_VERSION__: string;
